# React + Vite

1. Clear the css files and remove unwanted codes from app.jsx
2. Install external package required for the project
    - installed material ui
    - added bootstrap cdn link
    - added google fonts cdn link
    - installed react icons
    - updated global css in index.css file
    - updated website icon and title
3. Create a folder for pages. Inside create component files for each page
4. Create a folder for components. Inside create components that should appear in each page like header / footer.
5. Set up path for each pages by installing react-router-dom then linking the paths.
6. Design components


## 

## 
